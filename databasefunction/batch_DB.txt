use classdb;
drop TABLE if exists BATCH_DB;
CREATE TABLE IF NOT EXISTS BATCH_DB
(
`BATCH_NAME` VARCHAR(10) NOT NULL,
`REG_ID` int(10) unsigned NOT NULL DEFAULT '0',
`SUBJECTS` TEXT,
PRIMARY KEY (`BATCH_NAME`),
FOREIGN KEY (`REG_ID`) REFERENCES regtable(`REG_ID`)
)